# Issue 7: pinned built-in behavior coverage audit

## Scope and evidence boundary

Initial audit snapshot: **2026-09-17, inspection/verification through 04:35 UTC**, working tree based on `5b43fdaaab4f0d740fa00d4d4aeadc4e6fa3a0b3`. The per-name tables preserve that audit and its evidence distinctions. The follow-up gap table and [native acceptance verification](#native-acceptance-verification-2026-09-19) supersede the initial missing-evidence and CI-blocked observations.

Baseline: SDK **v0.0.115**, commit `1dc92b73900fac74dc357a938e4b5eee6392b418`; `docs/migration/ledger/sdk-v0.0.115/inventory.json`, tool records under `pkg/agentsdk/tools/`, plus the projected `crates/adk-tools/src/manifest.json`. The ledger's 62 tool-type records include wrappers, subagents and MCP; **62 is not the issue-7 name count**. The projection contains **46 distinct names / 51 variants**. Extra variants: Bash ×3 (`read_only`, `workspace_write`, `full_access`), Browser ×2 (`read_only`, `write`), Edit ×2 and Write ×2 (`workspace_write`, `full_access`). All other names occur once. Host-only names are Memory, save_plan, get_plan and the three skill tools; they must not disappear from the audit just because the runtime registry excludes them.

**Evidence labels:**

- **G-live**: a verifier executes the actual pinned Go tool and the Rust replay executable on the same input. The verifier exists; it was **not rerun by this audit**.
- **G-recorded**: Rust assertions consume output generated by actual pinned Go. Stronger than author-written expectations, but not a fresh Go run here.
- **G-checked**: authored expected values are separately checked by a Go verifier. Not a captured Go-output corpus or live Rust/Go comparison.
- **R**: authored Rust expectation, scripted peer or fake host. Not Go differential and not proof of OS containment.
- **M**: implementation definition comes from the same manifest against which it is checked. Useful drift protection, **not independent schema parity**.
- Fixture indices below are **zero-based JSON array indices**; `search.json` indices address `.cases`, state indices address `.steps`. A cited fixture is not automatically run by `cargo test`.

## Shared schema and policy evidence

`crates/adk-tools/tests/registry_matrix.rs::actual_sdk_runtime_feature_and_access_matrix` consumes **3,768** rows in `fixtures/tools/registry-matrix.json`. `registry-generate.go` invokes the actual Go runtime `BuildToolBundle`: **G-recorded name selection**, including strict/legacy feature modes, access, remote-write/private-network flags and mutation allowlists. It explicitly filters `host-only` out of the Rust comparison. It **does not compare schemas, approval behavior, dispatch, results or cleanup**.

In the tables, **REG** means these exact `crates/adk-tools/tests/registry.rs` tests (R, unless specified):

- `all_features_are_independent_and_names_are_unique_in_each_access_mode`
- `baseline_legacy_and_strict_modes_do_not_enable_opt_in_families`
- `browser_and_web_network_gates_are_distinct`
- `terminal_remote_write_and_async_shell_gates_cannot_be_allowlisted_away`
- `missing_implementations_are_reported_at_construction_not_tool_execution`
- `duplicates_are_not_silently_replaced`
- `supplied_contract_drift_is_rejected` (altered **think description only**, not every name/field)
- `explicit_extra_tools_are_feature_gated_and_prepared_with_the_same_policy` (new during audit; one synthetic extension).

**S0** in a schema cell means manifest-backed definition/REG construction only: no independently sourced whole-definition assertion for that name found. **S-state** means `registry.rs::state_definitions_match_every_static_baseline_contract` plus `crates/adk-project-state/tests/tools.rs::actual_go_tool_outputs_match_both_stores`, which compares **all 15 complete definitions** to actual Go fixture definitions. **S-shell** means `shell.rs::sdk_policy_and_schema_fixture` / `dynamic_schema_uses_only_trusted_environment`, using four Go-generated schema configurations in `shell.json` (not evidence for every static async/Terminal definition).

Every row lists happy/error/policy/schema/pagination-or-bounds/lifecycle evidence. `—` means no pagination or persistent resource contract, **not** a test pass. Policy name-selection coverage is not evidence that a captured executable handle rejects every forbidden invocation.

## Per-name map: filesystem and search (9 names / 11 variants)

Test paths in this and later tables are `crates/adk-tools/tests/<file>.rs::<test>` unless explicitly prefixed otherwise. Short aliases in cells are resolved immediately below each table.

| Name / variant | Happy result | Error result | Policy | Schema | Pagination / bounds | Lifecycle / side effects |
|---|---|---|---|---|---|---|
| read_file | SEARCH-read; `search.json` 2–5 | SEARCH-read; fixture 6–11 | SEARCH-confine, SEARCH-race; REG | S0 | SEARCH-read: line ranges/EOF; fixture 3–5 | SEARCH-race; no dedicated cancellation assertion identified |
| list_files | SEARCH-read; `search.json` 0–1 | SEARCH-read scalar input rejection | REG; no dedicated list path-confinement assertion identified | S0 | SEARCH-read; fixture 1 limit=2; no cursor API | No dedicated cancellation assertion identified |
| glob | SEARCH-pages, SEARCH-filters; fixture 12–19 | SEARCH-errors; fixture 20–22 | SEARCH-confine; REG | S0 | SEARCH-pages: continuation, limit changes, no duplicates, changed-query rejection, final cursor absent | No dedicated cancellation assertion identified (SEARCH-errors cancels grep only) |
| grep | SEARCH-context, SEARCH-filters; fixture 23–34, 39–50 | SEARCH-errors; fixture 35–38, 51–55 | SEARCH-confine; REG | S0 | SEARCH-pages for matches/files/count; SEARCH-context long lines/output omission | SEARCH-errors pre-cancellation |
| Write / workspace_write | WRITE-workspace; `lifecycle.json` 23–30 | WRITE-invalid | WRITE-alias; REG | S0 | No pagination | WRITE-workspace: atomic inode replacement/modes/parents; WRITE-cancel |
| Write / full_access | WRITE-full; `lifecycle.json` 31–38 | WRITE-invalid | WRITE-full: final symlink refused; REG | S0 | No pagination | WRITE-full: direct-write/hardlink semantics; WRITE-cancel |
| Edit / workspace_write + full_access | EDIT-exact loops **both** modes; `lifecycle.json` 39–76 | EDIT-multiple, EDIT-confine | EDIT-confine; REG | S0 | EDIT-multiple bounded/merged diff | EDIT-exact binary bytes/modes/CRLF; EDIT-confine cancellation/content preserved |
| Move | FS-happy; `lifecycle.json` 0–2 | FS-restore; fixture 6–12 | FS-confine, FS-readonly; REG | S0 | — | FS-restore restores source/no overwrite; FS-race |
| Delete | FS-happy; `lifecycle.json` 3–5 | FS-confine; fixture 13–22 | FS-confine, FS-readonly; REG | S0 | — | FS-happy only empty directories; FS-race |
| ApplyPatch | `patch.rs::pinned_sdk_fixtures` → `patch.json` **136** cases (G-recorded) | same fixture test; `confinement_and_no_partial_validation_changes` | `full_access_remains_confined_and_cancellation_precedes_mutation`; REG | S0 | `limits_and_bounded_output`; `aggregate_limit_and_linear_repeated_prefix_matching` | Fixture filesystem snapshots/modes, dry-run; validation-before-mutation test, cancellation test |

Aliases:

- SEARCH-read = `search.rs::read_ranges_bounds_listing_absolute_paths_and_suggestions`; SEARCH-pages = `complete_deterministic_pages_and_query_bound_cursors`; SEARCH-filters = `includes_excludes_double_star_and_gitignore_parent_rules`; SEARCH-context = `grep_context_counts_long_lines_and_omission_bounds`; SEARCH-confine = `confinement_refuses_symlinks_hardlinks_and_symlinked_ignore_files`; SEARCH-errors = `bounds_errors_and_cancellation_are_explicit`; SEARCH-race = `symlink_swap_during_read_cannot_escape`.
- WRITE-workspace = `write.rs::workspace_creates_parents_preserves_modes_and_replaces_inodes`; WRITE-alias = `workspace_rejects_escape_but_preserves_internal_alias_semantics`; WRITE-full = `full_access_retains_direct_write_semantics_and_refuses_final_symlinks`; WRITE-invalid = `invalid_inputs_and_directories_do_not_write_and_read_only_excludes_write`; WRITE-cancel = `cancelled_write_and_special_files_never_change_contents`.
- EDIT-exact = `edit.rs::exact_matching_preserves_binary_bytes_modes_and_line_endings`; EDIT-multiple = `multiple_matches_require_opt_in_and_diff_is_bounded`; EDIT-confine = `confinement_hardlinks_file_limits_and_cancellation_preserve_content`.
- FS-happy = `lifecycle.rs::moves_files_and_directories_and_deletes_only_empty_directories`; FS-restore = `failures_restore_source_and_never_replace_destination`; FS-confine = `rejects_links_special_files_and_path_traversal_without_touching_targets`; FS-readonly = `read_only_selection_cancellation_and_invalid_inputs_do_not_mutate`; FS-race = `parent_symlink_swaps_cannot_mutate_outside_workspace`.

`verify-search.go` is G-live: **56 cases / 80 calls**, including automatic cursor continuation. `verify-lifecycle.go` is G-live for **77 filesystem + 18 skills cases**, comparing results and trees/modes through `lifecycle_replay`. These fixtures contain inputs, not a checked-in Go expected-output array; the corresponding Rust integration tests are separately authored assertions. `error_only` weakens comparison to error status (plus filesystem effects for lifecycle), not exact diagnostic text. `verify-patch.go` can compare live and record Go output; `patch.rs` actually consumes recorded expected text/tree data. SEARCH-errors tests cancellation specifically on grep, not all four search names; shared implementation is not per-name assertion evidence.

Additional macOS assertions: `macos_filesystem.rs::{atomic_writes_and_edits_preserve_modes_and_isolate_hardlinks,move_never_overwrites_and_failures_restore_quarantined_sources,links_special_files_and_edit_limits_fail_without_mutation}`. Presence does not establish that these ran on this Linux audit host.

## Per-name map: shell, web, Browser, vision, LSP (9 names / 12 variants)

| Name / variant | Happy result | Error result | Policy | Schema | Pagination / bounds | Lifecycle |
|---|---|---|---|---|---|---|
| Bash / full_access | SHELL-sync (real Local processes, R) | SHELL-sync exit/timeout; SHELL-invalid | `shell_security.rs::explicit_full_access_behavior_is_preserved`; REG | S-shell | `shell.rs::output_cap_keeps_head_tail_without_killing_command` | SHELL-abort; SHELL-owner |
| Bash / workspace_write + read_only | `shell.rs::sdk_policy_and_schema_fixture` covers Go policy decisions, **not restricted process success**; `readonly_adapter_preserves_schema_and_uses_enforcing_backend` added | `restrictive_access_never_falls_back_to_local` | SHELL-security (static authorization expectations); REG | S-shell, readonly adapter | same buffer primitive; restricted success needs enforcing run | adapter fail-closed/error path is not containment success |
| BashStart | SHELL-async; `bash_start_reports_spawn_not_immediate_exit_status`; `bash_start_does_not_wait_for_quiet_job_output_or_exit` | `bash_start_reports_backend_setup_failure_without_job_id`; `async_timeout_and_backend_unavailability_are_reported`; SHELL-invalid | REG, `restrictive_access_never_falls_back_to_local` | S-shell | SHELL-async output retention | SHELL-owner; `caller_cancellation_stops_async_job_without_bundle_close` |
| BashPoll | SHELL-async running/completed output | SHELL-invalid unknown IDs; `async_timeout_and_backend_unavailability_are_reported` | REG | S0 | SHELL-async incremental/nonincremental; head/tail retention | timeout/cancellation and final job status in above tests |
| BashKill | SHELL-async | SHELL-invalid unknown IDs | REG | S0 | — | SHELL-async kill; SHELL-owner |
| Terminal | TERM-session, TERM-keys | TERM-invalid | `terminal.rs::no_terminal_without_full_access_and_remote_permission`; REG | S0 | TERM-cap, read/send output | TERM-control, TERM-cap awaited teardown; `shell.rs::terminal_secret_output_is_blocked_and_stays_blocked` |
| WebFetch | `web.rs::html_pagination_status_and_request_headers`; `web.json` 0–19 | same status test; `redirects_are_revalidated_and_compression_is_not_decoded` | `public_policy_cannot_be_overridden_by_arguments`; NET; REG | S0 | HTML test byte pages; fixture 1–5 UTF-8 boundary/negative/EOF offsets, 17 cap | `cancellation_and_deadline_close_pending_http_connections` |
| Browser / write + read_only | BROWSER-nav; BROWSER-shot only write; `browser.json` 0–7 | BROWSER-errors, BROWSER-policy; fixture 8–15 | BROWSER-policy; `prepared_browser_adapts_and_dispatches_the_same_readonly_contract`; REG | REG read-only action enum + prepared adaptation (M); no full Go-definition comparison | BROWSER-errors output truncation, dimensions; no page cursor | BROWSER-shot temp cleanup/atomic publish; `cancelled_browser_releases_runner_and_private_staging` |
| AnalyzeImage | VISION-fixture; URL MIME/header test | VISION-fixture; `url_read_failure_and_fifteen_second_request_timeout` | `filesystem_confinement_and_managed_absolute_paths`; `url_security_is_host_owned_and_revalidated_on_redirects`; NET; REG | `registry_contract_and_public_only_vision_are_not_browser_gated` (M) | `exact_file_byte_limit_and_oversize_preflight`; `url_byte_limit_is_enforced_without_relying_on_content_length` | `cancellation_and_deadline_interrupt_headers_and_body` |
| LSP | LSP-wire all eight normalized operations + aliases; LSP-Go normalization | LSP-route; `source_files_are_confined_regular_single_link_and_utf8`; LSP-timeout | `refuses_unconfined_executor_and_unsafe_environment`; `discovery_uses_the_same_confined_executor`; REG | LSP-Go compares full Go definition; LSP-route | LSP-timeout message/output/framing limits; `src/lsp_results.rs::tests::results_and_limits`; no cursor | `cancellation_deadline_and_host_close_interrupt_requests`; LSP-wire didOpen/didClose; LSP-diagnostics versions; **four integration tests can return early** |

Aliases:

- SHELL-sync = `shell.rs::bash_combines_streams_empty_exit_timeout_and_stdin_eof`; SHELL-async = `async_incremental_nonincremental_and_kill_contract`; SHELL-invalid = `invalid_input_and_unknown_jobs`; SHELL-abort = `aborted_synchronous_call_reaps_owned_process`; SHELL-owner = `bundle_close_and_owner_cancellation_reap_jobs` and `bundle_drop_cancels_even_if_tool_handles_survive`.
- SHELL-security = `shell_security.rs::{enforced_remote_write_bypasses_are_denied,restricted_shell_syntax_fails_closed_even_with_enforcement,git_requires_direct_invocation_and_explicit_nonprotected_destination,ordinary_build_and_test_commands_still_work_with_enforcement,io_descriptors_are_not_command_arguments_but_quoted_numbers_are}`. Despite names, inspect these as authorization tests, not proof an enforcing subprocess ran. `shell.json` is **168 Go policy cases + four schema configurations** generated by `shell-generate.go`; it does **not** oracle process output/job lifecycle/PTY behavior. Secret-output regression names are `shell.rs::{synchronous_secret_output_is_blocked,asynchronous_secret_split_across_polls_is_sticky_and_cancels,terminal_secret_output_is_blocked_and_stays_blocked}` (R).
- S-shell's fixture comparison specifically calls `shell::definition` for **FullAccess Bash and BashStart**, comparing input schemas and descriptions. It is not an independent definition comparison for both restricted Bash variants, BashPoll, BashKill or Terminal; restricted adapter assertions remain separately authored.
- TERM-session = `terminal.rs::pty_start_send_read_list_kill_and_persistent_state`; TERM-keys = `all_keys_and_verbatim_strings`; TERM-invalid = `invalid_operations_and_access_are_rejected`; TERM-control = `control_c_interrupts_foreground_and_eof_exits_shell`; TERM-cap = `terminal_output_is_capped_and_teardown_is_awaited`.
- BROWSER-nav = `browser.rs::navigation_text_and_launch_contracts`; BROWSER-shot = `screenshots_publish_atomically_preserve_modes_and_remove_temporaries`; BROWSER-errors = `failures_and_truncation_have_sdk_results`; BROWSER-policy = `policy_and_invalid_inputs_never_launch`. These use a **recording/fake runner**, not Chromium. `verify-browser.go` is G-live against `browser_replay`, also with a fake executor (16 inputs); `browser.json` is **not consumed by browser.rs**.
- VISION-fixture = `vision.rs::pinned_fixture_results_with_and_without_analyzer`, using 19 `vision.json` cases twice. `vision-verify.go` runs actual Go against the same authored expectation fields (**G-checked, 38 checks**), not live Rust/Go replay. `url_headers_mime_precedence_redirects_and_http_errors` is R. No analyzer must run: output is native prompt + image, even with an analyzer configured.
- LSP-route = `lsp.rs::schema_routing_validation_and_closed_lifecycle`; LSP-wire = `all_operations_sync_and_read_only_server_requests`; LSP-timeout = `timeouts_framing_limits_and_restarts`; LSP-diagnostics = `pull_push_fallback_and_versioned_diagnostics`; LSP-Go = **source unit test** `crates/adk-tools/src/lsp.rs::tests::pinned_go_differential_corpus`. `lsp-generate.go` uses a Go test overlay to execute pinned internal result parsing/operation normalization: **34 result cases + 15 operation inputs + definition** in `lsp-expected.json`. Error result cases compare failure classification, not exact error text; operation-normalization errors are exact. This does **not** compare language-server subprocess transport to Go. Scripted `src/lsp_protocol.rs::tests::{initialize_operations_and_document_notifications,published_diagnostics_require_current_version,fragmented_bounded_framing,framing_rejects_oversize_missing_duplicate_and_flood,session_drop_signals_stop_and_close_awaits_cleanup,transport_channel_eof_and_error_responses_fail_closed}` are R.
- NET = `crates/adk-tools/src/network.rs` tests `sdk_public_and_private_address_classes`, `url_credentials_local_aliases_and_schemes_are_rejected`, `mixed_dns_answers_are_rejected_before_dial`, `client_dials_only_pinned_addresses_without_dns_fallback`. `verify-web.go` is G-live, 20 local HTTP/HTML inputs, not a captured web-output corpus.

## Per-name map: Git (3 names)

All three use `git.rs::pinned_go_fixture_replay_exact_results_commands_artifacts_and_filesystem`: **118 G-recorded cases** in `git-expected.json`, generated by `git-generate.go` using actual Go tools with fake command runner/temp repositories. This does not establish real Git/GH process confinement or live GitHub behavior.

| Name | Happy result / fixture names | Error result / fixture names | Policy | Schema | Pagination | Lifecycle |
|---|---|---|---|---|---|---|
| create_pull_request | `pr-dirty-title-draft`, `pr-untracked-default-message`, `pr-existing`, `pr-fallback`, `pr-body-only`, `pr-title-body`; host attribution test | `pr-create-failure`, `pr-error-add -A`, `pr-error-commit --no-verify -m changes from agent run`, `pr-error-push --no-verify -u origin HEAD`; authorization test | `pr-guard-*`; GIT-gates; REG remote gate | `definitions_registry_and_timeout_parity` (M) | — | `host_attribution_and_nonfatal_artifact_failure`; GIT-host cancellation |
| create_github_issue | `issue-simple`, `issue-normalize-labels`, `issue-existing-labels`, `issue-unicode-labels` | `issue-label-list-error`, `issue-label-create-error`, `issue-output-bad`, `issue-label-edge-*` | GIT-gates; REG (do not infer PR remote-write gate applies to issue creation) | same definitions test (M) | — | artifact-failure test; GIT-host cancellation |
| attach_repository | `attach-defaults`, `attach-repo-alias`, `attach-fallback`, `attach-existing`; `missing_workspace_created_through_host_hook` | `attach-fallback-fails`, `attach-checkout-error`, `attach-origin-*`, `attach-url-*` | GIT-gates; `host_rejects_symlink_repo_paths_before_commands`; REG | same definitions test (M) | — | `cancelled_inflight_clone_cleans_partial_checkout_without_retry`; fixture `attach-clone-*`, `attach-incomplete`; GIT-host cleanup |

GIT-gates = `git.rs::owner_gates_and_operation_cancellation_prevent_commands` and `runner_authorization_errors_are_not_pr_recovery_or_incomplete_clone_signals`.

GIT-host = `git_host.rs::{trusted_full_access_executes_real_git_and_requires_host_attribution_policy,local_backend_never_upgrades_restricted_caller_or_host_permissions,unavailable_enforcing_backend_does_not_fall_back_to_local,executor_uses_explicit_paths_clean_environment_and_program_specific_output,cancellation_and_shorter_deadlines_are_propagated,pending_host_authorization_and_attribution_obey_cancellation_and_deadline,trusted_git_commit_keeps_sandbox_hook_disabling_and_host_attribution,cleanup_is_scoped_to_partial_roots_and_invocation_and_never_follows_links,cleanup_rejects_replaced_roots_and_does_not_leave_quarantine,filesystem_rejects_readonly_and_wrong_workspace_but_authorized_cleanup_survives_cancellation,directory_symlink_swap_race_never_writes_outside_workspace}`. These are adapter tests, not an end-to-end real create-PR/create-issue/attach workflow against GitHub.

## Per-name map: signal, plans, skills and namespace Memory (10 names)

| Name | Happy result | Error result | Policy | Schema | Pagination / limits | Lifecycle / persistence |
|---|---|---|---|---|---|---|
| think | SIGNAL-fixture `signals.json` 0 | fixture 1–2; SIGNAL-types | REG; SIGNAL-types pre-cancelled think | S0 | — | no durable effect; cancellation only |
| AskUserQuestion | SIGNAL-fixture 3–5, Unicode escaping | SIGNAL-types | REG | S0 | — | fixture pause flag and choices/freeform defaults |
| present_plan | SIGNAL-fixture 6–7 (legacy mode stripped) | fixture 8–9; SIGNAL-types | REG | S0; stripping legacy mode is not proof schema omits mode | — | pause/recommended fallback fixture |
| finish | SIGNAL-fixture 10–11 | SIGNAL-types; SIGNAL-fail failing sink | REG | S0 | — | SIGNAL-fail verifies failed sink never pauses; default fixture is not successful persistent sink evidence |
| save_plan | PLAN: chosen summary/UTF-8 byte count | PLAN missing/typed plan; SIGNAL-fail persistence failure | PLAN checks control-flow flag and host identity; REG host-only | S0 | PLAN 210-byte plan truncates summary at 200 bytes | Recording in-memory ArtifactStore, not durable host restart |
| get_plan | PLAN absent plan + round-trip, ignores arguments | SIGNAL-fail read failure | PLAN host identity; REG host-only | S0 | — | same recording store, no nil/empty-content equivalence assertion |
| skill_search | SKILL-search; `skills.json` 0–5, 16–17 | SKILL-search query wrong type | SKILL-absence RO selection; REG host-only | S0 | no pagination API; catalog order/query precedence | host env status via SKILL-env; no process launch |
| skill_install | SKILL-install; `skills.json` 6–9, 11, 14–15 | SKILL-absence unknown skill/invalid config/path cases | SKILL-absence prepared RO exclusion; REG host-only | S0 | — | SKILL-install merge/mode/hardening; SKILL-env cancellation before creation |
| skill_list_installed | SKILL-install; `skills.json` 10, 12–13 | SKILL-absence | SKILL-absence; REG host-only | S0 | no cursor; installed versus non-catalog servers | reads `.mcp.json`; ignores irrelevant input |
| Memory | `memory.rs::host_namespace_identity_and_all_memory_actions` all four actions + empty results | `malformed_requests_and_store_errors_do_not_succeed` missing fields/unknown action/bad ID/missing memory/invalid namespace | first test host namespace/source/repo spoof resistance + other-namespace isolation + prepared RO exclusion; REG host-only | S0 | first test `limit:1` over **one record**: does not establish truncation/defaults or real tag exclusion | InMemoryStore only; store/list/search/delete sequence, not persisted host restart; no cancellation test found |

SIGNAL-fixture = `signals.rs::deterministic_source_derived_signal_results`; `signals.json` has 12 authored expected results separately checked against actual Go by `verify-signals.go` (**G-checked**). SIGNAL-types = `type_errors_do_not_pause_and_cancelled_calls_do_not_execute` (malformed scalar for registry signal tools; cancellation specifically think). PLAN = `plan_artifacts_use_host_identity_byte_counts_and_source_summary_rules`. SIGNAL-fail = `artifact_failures_are_not_success_and_finish_sink_failures_do_not_pause`.

SKILL-search = `skills.rs::search_preserves_order_query_precedence_and_environment_status`; SKILL-install = `install_merges_hardening_and_lists_without_starting_servers`; SKILL-absence = `absence_errors_confinement_and_read_only_surface`; SKILL-env = `cancellation_and_host_environment_names_are_respected`. Go comparison is through `verify-lifecycle.go`, `skills.json` and `skill-catalog.json`, not an automatic fixture replay in skills.rs.

## Per-name map: all 15 project-state tools

**STATE-R** = `crates/adk-tools/tests/state.rs::all_fifteen_tools_integrate_with_durable_filesystem_and_sqlite_stores`; actual registry + filesystem/SQLite, all 15 names invoked. **STATE-contract** = `crates/adk-project-state/tests/tools.rs::baseline_state_tool_contracts_on_filesystem_and_sqlite`. **STATE-Go** = same file `actual_go_tool_outputs_match_both_stores`; **98 steps**, generated by actual Go in `fixtures/project-state/tools.go` → `tools.json`.

STATE-Go normalizes generated IDs, timestamps and comment IDs; compares JSON values/text/error/pause. `Invalid input:` cases check only that prefix, **not Go's full diagnostics**. STATE-R has a common tool-output success assertion; do not mistake every invoked mutation for a dedicated field assertion.

**P-state** = STATE-R reopens read-only registry and asserts prepared list is exactly `memory_list`, `memory_recall`, `memory_stats`, `prime_context`, `task_ready`, `task_show`; STATE-contract checks approval=false and the six read-only flags. This is not per-name cancellation or captured-handle denial. **L-state** = both stores run the workflow; STATE-R specifically reopens and reads stored memory and closed task; STATE-contract reopens and reads updated memory. No per-name shutdown resource semantics are implied.

| Name | Happy result: STATE-Go step(s), STATE-contract/STATE-R | Error result: STATE-Go steps | Policy | Schema | Pagination / filter evidence | Lifecycle |
|---|---|---|---|---|---|---|
| task_create | 0–1 default/zero priority; 20 nullable fields | 31–32,36,38–41 empty/null/type input | P-state | S-state | — | L-state |
| task_ready | 3,9 blocker closing changes readiness; 21 null filters | 37,42–45 type errors | P-state | S-state | no multi-row limit/assignee/label filter assertion | L-state |
| task_show | 11 closed task | 46–49 malformed top-level; STATE-contract numeric ID | P-state | S-state | — | L-state reopened closed status |
| task_update | 6–7 label set/clear; 22,28 null behavior | 50–53 malformed top-level | P-state | S-state | — | L-state; label clear asserted |
| task_claim | 4 trimmed host actor | 54–57 malformed top-level | P-state | S-state | — | L-state; no competing claim through tool boundary |
| task_close | 8 reason/status | 58–61 malformed top-level | P-state | S-state | — | L-state; readiness transition |
| task_comment | 5 trimmed host actor/body | 62–65 malformed top-level | P-state | S-state | — | L-state; comment ID/time normalized |
| task_link | 2 add default; 10 remove | 66–69 malformed top-level | P-state | S-state | — | L-state; dependency affects readiness |
| memory_remember | 12 typed semantic/user; 23 nullable defaults | 33 empty content,70–73 malformed | P-state | S-state | — | L-state |
| memory_recall | 15 query; 26 null filters | 74–77 malformed top-level | P-state | S-state | only single matching memory; no limit competition/tag exclusion | L-state |
| memory_list | 14 kind filter; 25 null filters | 78–81 malformed top-level | P-state | S-state | positive singleton filter only; no competing kinds/tags/limit | L-state reopened memory |
| memory_update | 13 preserves scope and clears tags; 24,27 null handling | 34 missing ID,35 null ID,82–85 malformed; STATE-contract deleted ID does not resurrect | P-state | S-state | — | L-state preserved omitted fields/reopen |
| memory_delete | 18 followed by empty stats 19 | 86–89 malformed top-level | P-state | S-state | — | L-state; no missing-ID/not-found tool result assertion |
| memory_stats | 16 by-kind/scope/tag result; 19 empty; 29 null input | 90–93 malformed top-level | P-state | S-state | no filtered mixed-population counts | L-state post-delete total |
| prime_context | 17 active task/null limits; 30 null input; STATE-R contains dependent title | 94–97 malformed top-level | P-state | S-state | no competing memory/ready limits or unknown active-task assertion | L-state (rendered context, not another persisted object) |

Store-level tests in `crates/adk-project-state/tests/contracts.rs` and `fixtures/project-state/{expected.json,namespace-memory.json}` are complementary persistence/filter evidence, **not substitutes for tool argument forwarding, result formatting, error wrapping or registry behavior**. The 15 lower-case memory/task/prime tools and upper-case namespace `Memory` are different interfaces and stores.

## Follow-up verification of the audit gaps

The initial audit identified concrete gaps; the following work supersedes its
missing-evidence observations in the per-name tables above.

| Gap | Added evidence / status |
|---|---|
| Runtime composition and dispatch | `tests/bundle.rs` and `crates/adk/tests/tool_runtime.rs` pass. The bundle module is exported. The facade replaces model and dispatch lists together, preserves approval policy, invalidates handles on close/drop, and tests stream cancellation. |
| Access narrowing and exact grants | Bundle regressions prove full-access Write/Edit cannot escape workspace-write preparation, including allowlisted names. Read-only exact exceptions remain functional. `shell.rs::prepared_exact_bash_grant_preserves_but_never_raises_host_access` checks effective dispatch under all three host access ceilings. |
| State filters, binding limits, errors and actor overrides | `state.rs::pinned_go_filters_limits_semantic_errors_and_actor_overrides_on_both_stores` replays all 51 calls against both durable backends. All 102 outputs match; nine real differences per backend were fixed, not normalized away. |
| Namespace Memory | `memory.rs::pinned_go_nullable_fields_tags_and_padded_uuid_delete` checks actual Go results; `recording_store_proves_forwarding_binding_limits_defaults_and_tag_exclusion` uses 55 competing records and records forwarded arguments. Missing host injection is explicitly tested. |
| Plan artifacts | `signals.rs::pinned_go_empty_artifact_and_summary_byte_boundaries` covers actual Go empty/absent and 200-byte/UTF-8 boundaries. Host injection remains mandatory by type and is tested separately from Go nil-store errors. |
| Independent schemas | `schema-generate.go` never reads the Rust manifest. Twelve actual Go bundles yield 500 instances covering all 46 names / 51 variants across access and four shell environments. Nine `tests/schema.rs` tests compare names, descriptions, parameters, mutability, approval, control-flow metadata and timeouts. This independent evidence supersedes every S0/M schema limitation above. |
| Per-name search cancellation/confinement | `search.rs::every_search_name_rejects_escape_and_pre_cancelled_execution` explicitly checks all four names, supplementing existing per-tool success/error/pagination tests. |
| Owned process cleanup | Native Local Browser tests prove immediate reaping after bundle close, including abandoned calls and interrupted close. LSP completion tracking survives removed/abandoned sessions; its enforcing bundle-close regression is required in CI. Git runner close also exposes an awaited cleanup boundary. |
| Fresh Go verifiers | Actual pinned verifiers passed: 12 signals, 80 search calls/56 cases, 95 filesystem+skills, 1,176 ApplyPatch cases, 20 WebFetch, 16 Browser and 38 vision results. LSP has 50 independent result/operation/schema checks; Git has 118 recorded command/result/artifact/filesystem cases. |

No live-GitHub, public-network or real-analyzer test is required for the
host-adapter contracts. Fake boundaries are identified as such; native Chrome
rendering and live GitHub actions are not claimed.

The Git replay module is Unix-only because its pinned Go command/result corpus
contains native Unix paths, including JSON-encoded paths. Windows still compiles
the Git library and runs the registry/schema and unsupported-target checks; the
Unix corpus is not asserted to encode Windows-native path rendering.

## Enforcing skips and fresh verification status

- `crates/adk-tools/tests/lsp.rs::available` returns false and prints `SKIP confined LSP integration` on backend failure unless **`ADK_REQUIRE_SANDBOX` is present**, in which case it asserts. Four tests return early: `all_operations_sync_and_read_only_server_requests`, `pull_push_fallback_and_versioned_diagnostics`, `timeouts_framing_limits_and_restarts`, `cancellation_deadline_and_host_close_interrupt_requests`. Cargo may print them as **passed**, not ignored, after that return.
- `crates/adk-sandbox/tests/os_backends.rs::os_enforcement_required_in_ci` uses the same fail-required/otherwise-SKIP pattern. Linux/macOS `cfg` guards omit unsupported filesystem/LSP tests entirely; shell/Terminal are Unix-only; macOS-specific tests do not execute on Linux.
- `.github/workflows/execution-security.yml` sets `ADK_REQUIRE_SANDBOX=1` for `cargo test --locked -p adk-sandbox -p adk-security -p adk-tools --all-targets` on Ubuntu/macOS. It installs Bubblewrap and adjusts the disposable Linux runner namespace policy. The subsequent public `adk` execution-feature step does **not** inherit that step-scoped environment. Windows checks compile/fail-closed behavior, not Unix containment.
- Existing `lsp-notes.md` reports four explicit local skips caused by Bubblewrap `/proc/sys/kernel/overflowuid` unavailability. That is historical handoff evidence, **not a fresh result here**.
- Historical initial audit command: direct Rust 1.97.1 Cargo with `RUSTFLAGS='-C linker-features=-lld -C link-arg=-fuse-ld=bfd'`, `cargo test -p adk-tools -p adk-project-state --all-targets -- --nocapture`. **Exit 101**, compilation stopped at `crates/adk-tools/tests/bundle.rs:2`, unresolved `adk_tools::bundle`. No all-target pass count is claimed. Plain `cargo --version` through the rustup shim also failed (`no /proc/self/exe available`); direct toolchain execution bypassed that host limitation. The repository pins 1.88.0; this attempt is not evidence for the pinned toolchain.
- Historical focused audit rerun with the same direct toolchain: `cargo test -p adk-tools --test signals --test memory --test state --test registry_matrix -- --nocapture` **passed 8 tests, 0 failed, 0 ignored**: signals 4, Memory 2, state 1, registry matrix 1. This exercised the 3,768 selection rows and the registry-backed filesystem/SQLite workflow; it did **not** run the separate project-state Go-output suite or any enforcing subprocess tests. The later parent integration resolved that compile failure.
- A mechanical document check found rows for **all 46 manifest names / 51 variants**, with no missing names. Test-identifier spelling was cross-checked against Rust sources; this checks references, not behavioral completeness.

The ledger's own `verification_status` fields are historical inventory metadata, not fresh test verdicts. Reconcile this document against the parent's final test snapshot before closing issue 7.

Parent integration verification (2026-09-17): `cargo test --locked --workspace --all-features --all-targets` passed on the pinned Rust **1.88.0** (580 harness passes, 0 failures, 2 ignored helpers, 77 targets) after bundle exports, independent schemas, state corrections, and Browser/LSP/Git cleanup fixes. All six regenerated Go oracle files matched their prior SHA-256 hashes. The shell corpus contains 168 policy cases and four environment-specific schemas. Pinned rustfmt, strict Clippy, warning-free rustdoc, doctests, manifest comparison, dependency purity and Python replay tests also pass. Two new-code Clippy warnings were corrected without changing behavior.

Locally unavailable enforcing tests can still return early; these counts are **not** a claim that native OS checks ran here. Required Linux/macOS CI remains the authority for those checks. On implementation head `03ad3a76b39213769fc0ab98354b2027f1d1593a`, all 20 GitHub check records failed or were cancelled within seconds. Logs for both enforcing jobs and the quality job were unavailable (`log not found`), so no code-level diagnosis or native verification can be inferred from those failures. The prior macOS launcher fix remains unverified; Windows unsupported-target behavior likewise still requires a functioning runner. Keep PR #23 draft and issue #7 open until fresh native checks pass.

## Native acceptance verification (2026-09-19)

The infrastructure blockage above is resolved. Implementation head
`728f169aca907547005279048a0cb226d94b9136` passed **all 20 push/PR checks**.
This snapshot covers the complete implementation, not a demo subset.

| Fresh pinned Rust 1.88 CI evidence | Result |
|---|---|
| [All workspace features/targets](https://github.com/gratefulagents/adk/actions/runs/35471218710/job/105972410166) | 582 passed, 0 failed, 2 ignored helpers; 77 targets |
| [Required Linux enforcement](https://github.com/gratefulagents/adk/actions/runs/35471218702/job/105972410211) | 250 passed, 0 failed, 1 ignored subprocess helper; 36 targets; public execution feature adds 3 passes |
| [Required macOS enforcement](https://github.com/gratefulagents/adk/actions/runs/35471218702/job/105972410178) | 250 passed, 0 failed, 2 ignored subprocess helpers; 36 targets; public execution feature adds 3 passes |
| [Windows unsupported-target and state](https://github.com/gratefulagents/adk/actions/runs/35471218702/job/105972410117) | 90 passed, 0 failed, 0 ignored; 40 targets |
| Quality and dependency jobs | fmt, strict Clippy, warning-free rustdoc, doctests, dependency licenses/advisories, purity, manifest and replay checks pass |

Ignored helper entry points are invoked explicitly by their parent enforcement
or independent-process tests; they are not omitted security scenarios. Both
native enforcement jobs set `ADK_REQUIRE_SANDBOX=1`. The Windows job checks
unsupported confinement fails closed and exercises durable state; it does not
claim Unix process/filesystem containment on Windows.

Fresh pinned-Go execution verified 12 signal results, 80 search calls across 56
cases (including pagination), 95 filesystem/skills cases, 1,176 patch cases,
20 WebFetch cases, 16 Browser cases and 38 vision results. Regeneration of all
six Git/LSP/registry/schema/shell/state-memory-plan oracle files was byte-identical
(SHA-256/cmp). These comprise 118 Git cases, 50 LSP checks, 3,768 registry rows,
500 independent schema instances across 12 bundles, 168 shell policy cases,
and 68 state/namespace-Memory/plan cases. The 51 state-tool calls are replayed
against both filesystem and SQLite stores: 102 matching outputs. All 15 state
tools also retain the separate baseline project-state contract suite.

Native failures were resolved without dropping test families or security cases:

- Real FIFO fixtures use portable `mkfifo`; symlink fixtures initialize the
  recorded mode on macOS instead of ignoring permission comparisons.
- Windows retains regular-file synchronization, uses Unix-only directory fsync,
  preserves exact fixture bytes, and recovers only demonstrably dead lock owners.
- Seatbelt grants only the selected Xcode license receipt and metadata on ancestors
  of explicit read roots, never broader content or write grants. Explicit non-self
  process-info denial closes the observed parent-environment leak; the original
  native leak regression passes.
- Cancellation during Seatbelt setup keeps its cancellation/timeout category,
  and LSP teardown does not misreport a never-started cancelled server as a cleanup
  failure. Early-cancellation, active-server, abandoned-call and bundle-close tests
  pass natively.
- macOS CI uses canonical temporary workspace paths; LSP fixtures use canonical
  protocol URIs and an existing system probe executable. Protocol-rejection cases
  no longer inherit the separate startup-timeout case's deliberately short timer.
- Shell line-number parity is checked against the actual native Bash, accounting
  for Apple Bash 3.2's different `-c` numbering without changing tool output.

All baseline families now have implemented behavior and mapped happy/error/policy,
limit/pagination, schema and lifecycle evidence. No registered placeholder or
missing selected implementation is silently tolerated. External-adapter limits
remain explicit: no live GitHub, native Chrome rendering, public-network service
or third-party analyzer execution is claimed. This is regression-backed acceptance
of the pinned contracts, not a proof over every possible input or host environment.
